import ProfileField from './ProfileField'

function TeachingCertificates(props) {
    return (
        <ProfileField icon={props.icon} placeholder={props.placeholder} title={props.title}></ProfileField>
    );
}

export default TeachingCertificates;